# [프로젝트명] — FE/BE 페어 기능 단위 구현 명세서

> 생성 스킬: impl-fe-be-doc
> AI Agent 및 개발자 참조용
> 작성일: {YYYY-MM-DD}
> 갱신일: {YYYY-MM-DD}            <!-- 갱신 시에만 작성/갱신. 신규면 작성일과 동일하게 두거나 생략 -->
> 작성자/갱신자: {git_user 또는 사용자명}  <!-- 다른 사용자가 갱신한 경우 "원작성자 → 갱신자" 형식으로 보존 -->
> `[기술 스택 태그]`

---

## 읽는 법

- **태스크 ID** : `BE-XX` / `FE-XX` / `INF-XX` 형식. 전체 문서 통합 일련번호.
- **의존** : 시작 전 반드시 완료되어야 하는 태스크. 없으면 "없음".
- **Agent 지시** : 구현 시 따라야 할 핵심 구조 힌트. 완성 코드 아님. 있을 때만 포함.
- **✅ 통합 검증** : Phase 내 BE+FE 페어 완료 후 수행. 합격해야 다음 Phase로 이동.

---

<!-- Phase 0: 프로젝트 전체 스케일에서만 포함. 기능/화면/컴포넌트 단위면 이 Phase 전체 삭제 -->
## Phase 0 — 인프라 & 개발 환경 세팅

### INF-01 · [태스크명] (`[파일 경로]`)
**의존** : 없음

[구현 내용]

**검증 기준** : [단독 완료 판단 조건]

---

<!-- Phase 1: 가장 단순한 BE-FE 통신 파이프라인 전체를 검증하는 Phase -->
## Phase 1 — [기능명] ([성격])

<!-- 화면 중심 Phase인 경우 이 블록을 포함 -->
### 화면 개요

- **대응 SFR/REQ**: [SFR-XXX / REQ-XXX]
- **목적**: [이 화면이 존재하는 이유]
- **사용자**: [사용자 역할]
- **진입 경로**: [어디서 이 화면에 오는가]
- **이탈 경로**: [이 화면에서 어디로 가는가]

### 컴포넌트 구조

```text
[화면명]Page
├── [공통 레이아웃]
├── [컴포넌트A]
└── [컴포넌트B] (조건부)
```

### API 연동

| # | 메서드 | 경로 | 호출 시점 | 요청 | 응답 | 에러 처리 |
|---|--------|------|----------|------|------|----------|
| 1 | [GET/POST] | [/api/...] | [마운트/클릭/제출] | [query/body] | [응답 구조] | [처리 방식] |

### 상태 관리

| 상태 | 유형 | 위치 | 설명 |
|------|------|------|------|
| [상태명] | [로컬/서버/전역] | [컴포넌트/스토어/캐시] | [용도] |

---

### BE-01 · [태스크명] (`[파일 경로]`)
**의존** : [선행 태스크 ID 또는 없음]

[구현 내용]

**Agent 지시** : (있을 때만)
```
[핵심 구조 힌트만 — 완성 코드 금지]
```

**검증 기준** : [단독 완료 판단 조건]

---

### FE-01 · [태스크명] (`[파일 경로]`)
**의존** : [BE-XX]

[구현 내용]

**검증 기준** : [단독 완료 판단 조건]

---

### ✅ Phase 1 통합 검증

**시나리오**: [한 줄 요약]

1.
2.
3.

**합격 조건** : [조건 A] ✅ / [조건 B] ✅

<!-- 화면 중심 Phase이면 기능/상태/에지 케이스 체크리스트를 추가 -->
#### 화면 검증 체크리스트

- [ ] [SFR/REQ] [데이터 조회]: [구체적 확인 방법]
- [ ] [SFR/REQ] [로딩/에러/빈 상태]: [구체적 확인 방법]
- [ ] [SFR/REQ] [권한/대량 데이터/중복 제출]: [구체적 확인 방법]

---

<!-- Phase N: 핵심 기능 Phase. 설계 문서 G섹션 기준으로 가장 상세하게 작성. -->
## Phase N — [기능명]

<!-- 이하 동일 구조 반복 -->

---

<!-- 마지막 Phase: 배포 환경이 있는 경우에만 포함 -->
## Phase [N+1] — 배포 환경

<!-- Dockerfile, compose.yaml 등 배포 관련 태스크만 -->

---

## 전체 Phase 의존 흐름

```
Phase 0 (인프라)
  └─ Phase 1 ([기능명])
       └─ Phase 2 ([기능명])
            └─ Phase N (배포)
```

---

## Agent 필독 — 전역 주의사항

<!-- 설계 문서 주의사항 + pitfall-checklist 해당 항목만 추출. 없으면 섹션 삭제. -->

> ⚠️ [기능/동작 주의]

> 🛡️ [보안]

> 💡 [운영 팁]

---

## 미결 사항

<!-- 설계 문서 열린 결정 사항 + 인터뷰 중 미결된 것. 없으면 섹션 삭제. -->

| 항목 | 옵션 | 결정 시점 |
|------|------|----------|
| | | 🔴 [태스크 ID] 전 필수 / 🟡 나중에 가능 |
