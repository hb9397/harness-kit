# 함정 체크리스트 (pitfall-checklist)

작업지침서 초안 완성 후 출력 전에 아래를 자체 점검한다.
**스케일에 따라 해당하는 섹션만 실행한다.**

| 스케일 | 실행할 섹션 |
|--------|------------|
| 프로젝트 전체 | 기술적 함정 + 문서 함정 + 구조적 함정 전체 |
| 기능 단위 | 기술적 함정 + 구조적 함정 |
| 화면 단위 | 기술적 함정(FE 관련만) + 구조적 함정 |
| 컴포넌트/로직 | 구조적 함정만 |

---

## 기술적 함정 (범용)

```
[ ] 외부 API 호출에 타임아웃·재시도 전략이 명시되어 있는가?
[ ] 비동기 처리(async/await, 큐)가 필요한 곳에 동기 처리가 가정되어 있지 않은가?
[ ] 환경별 설정값(개발/배포)이 하드코딩 없이 환경변수로 분리되는가?
[ ] 인증/인가가 필요한 엔드포인트에 누락이 없는가?
[ ] 대용량 데이터 처리 시 페이징/스트리밍 전략이 있는가?
```

## 기술적 함정 (프로젝트 커스텀)
<!-- 프로젝트별 특수 기술 스택 체크리스트를 여기에 추가한다 -->
<!-- 예시: Playwright, LangGraph, WebSocket 등 프로젝트 고유 기술은 이 섹션에만 -->
<!--
[ ] 클릭 캡처 방식: JS 주입(CDP) vs Playwright 이벤트 리스너 — 설계 문서 기준 확인
[ ] 로그인 자동화 여부: 세션 쿠키 재사용인지, 실제 자동화인지
[ ] headless 값 하드코딩 여부: 반드시 환경변수로 분기해야 함
[ ] LLM 클라이언트 직접 호출 여부: 팩토리/추상화 레이어 필요 여부
[ ] DOM 크기 제한 여부: LLM에 전체 HTML 넘기면 토큰 초과 위험
[ ] CORS 설정 여부: FE-BE 다른 포트라면 필수
[ ] WebSocket 엔드포인트 분리 여부: 기능별로 분리되어 있는지
-->

---

## 문서 함정

```
[ ] API 경로 표기 오류: 설계 문서의 엔드포인트 목록과 대조
[ ] 환경별 동작 차이 누락: 개발/배포 환경에서 다른 동작은 반드시 명시
[ ] DB 모델 누락: 설계 문서에 있는 모든 테이블이 BE-XX에 반영됐는지
[ ] 마이그레이션 태스크 누락: DB 모델 정의 다음에 반드시 포함
[ ] .gitignore 생성 태스크 누락: 세션 파일, .env 파일 보안 처리
[ ] noVNC 분기 누락: 개발/배포 환경에서 FE 렌더링이 다른 경우
[ ] start.sh 환경 혼용: 배포 전용임을 명시
```

---

## 구조적 함정

```
[ ] Phase가 너무 큰가? — 한 Phase에 API 5개 이상이면 쪼갠다
[ ] 페어가 아닌가? — BE만 있거나 FE만 있는 Phase는 합치거나 분리
[ ] 검증이 너무 늦은가? — 핵심 기능은 최대한 앞 Phase에서 검증
[ ] 미결 사항이 구현을 막는가? — 결정 필요 항목은 해당 태스크 전에 표시
[ ] 태스크 의존 관계가 순환하는가? — A→B→A 형태의 순환 의존 금지
```

---

## 화면 중심 구현 함정

```
[ ] 화면별 대응 SFR/REQ가 누락되지 않았는가?
[ ] 컴포넌트 책임이 과도하지 않은가? — 1개 컴포넌트가 API 호출 + 렌더링 + 상태 관리를 모두 하면 분리
[ ] Props drilling이 3단계 이상인가? — Context 또는 상태 관리 라이브러리 도입 검토
[ ] 목록 조회에 페이징/정렬/필터 기준이 있는가? — 없으면 대용량에서 성능 문제
[ ] API 응답 형식이 화면 필요 데이터와 일치하는가? — 변환 레이어 필요 여부
[ ] 서버 상태와 로컬 상태가 혼재되어 있지 않은가?
[ ] 로딩/에러/빈 상태가 각 화면에 정의되어 있는가?
[ ] 화면 이동 후 돌아왔을 때 유지해야 할 상태가 명시되어 있는가? — 필터, 스크롤 위치
[ ] 라우팅 가드(인증 체크)가 누락된 화면이 없는가?
[ ] 구현 순서대로 진행했을 때 이전 Phase 없이 작동하지 않는 화면이 없는가?
```
