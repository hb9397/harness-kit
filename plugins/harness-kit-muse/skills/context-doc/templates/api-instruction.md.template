# API 설계 지침

> 이 문서는 `[프로젝트명]`의 HTTP API를 일관되게 설계·구현하기 위한 정본이다.
> 보통 엔드포인트 명명, 요청·응답 schema, 상태 코드·오류 형식, 인증·인가, 버전 관리와
> 공개 계약의 호환성 기준을 다룬다.

<!--
HTTP API의 존재만으로 만들지 않고 독립해서 반복 적용할 현재 규칙이 확인될 때 생성한다.
최초에는 위 목적 설명만 두고 이후 현재 계약과 승인된 규칙만 추가하며, 폐기된 endpoint와
변경 이력은 남기지 않는다.
-->
