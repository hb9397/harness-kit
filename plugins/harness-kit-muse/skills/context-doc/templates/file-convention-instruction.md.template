# 파일 구성 지침

> 이 문서는 `[프로젝트명]`에서 새 파일과 디렉터리를 일관된 위치와 이름으로 관리하기
> 위한 정본이다. 보통 폴더별 배치 책임, 파일명 규칙, 한 파일의 책임, 진입점·특수 파일
> 경계와 새 디렉터리 추가 기준을 다룬다.

<!--
최초에는 위 목적 설명만 둔다. 이후 현재 패키지·파일 구조와 사용자가 확정한 규칙만
추가하고, 존재하지 않는 경로나 과거 배치와 변경 이력은 남기지 않는다.
-->
