# 비-HTTP 통신 지침

> 이 문서는 `[프로젝트명]`의 WebSocket·메시지 큐·RPC·이벤트 발행 같은 비-HTTP
> 통신을 일관되게 구현하기 위한 정본이다. 보통 메시지 schema·타입·방향, 발행 조건,
> 순서·중복·재시도·재연결·실패 처리와 호환성 기준을 다룬다.

<!--
비-HTTP 통신의 존재만으로 만들지 않고 독립해서 반복 적용할 현재 규칙이 확인될 때
생성한다. 최초에는 위 목적 설명만 두고 이후 현재 프로토콜과 승인된 규칙만 추가하며,
폐기된 메시지와 변경 이력은 남기지 않는다.
-->
