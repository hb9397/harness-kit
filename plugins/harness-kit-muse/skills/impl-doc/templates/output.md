# impl-general 범용 작업지침서 템플릿

아래 양식으로 산출물을 작성한다. `<!-- 지침 -->` 주석은 작성 후 제거한다.

---

```markdown
# 구현 작업지침서: {프로젝트명}

> 생성 스킬: impl-doc
> 프로젝트 유형: {CLI / 라이브러리 / 서비스 / 스크립트 / 파이프라인 / AI Agent / BE 단일 기능 / FE 단일 기능}
> 설계 문서: {참조 파일명}
> 작성일: {YYYY-MM-DD}
> 갱신일: {YYYY-MM-DD}            <!-- 갱신 시에만 작성/갱신. 신규면 작성일과 동일하게 두거나 생략 -->
> 작성자/갱신자: {git_user 또는 사용자명}  <!-- 다른 사용자가 갱신한 경우 "원작성자 → 갱신자" 형식으로 보존 -->
> 기술 스택: {언어, 프레임워크}

---

## 전역 주의사항

<!-- 전체 Phase에 걸쳐 지켜야 할 규칙. 설계 문서의 주의사항 섹션에서 가져온다. -->

- {주의사항 1}
- {주의사항 2}

---

## 미결 사항

<!-- 설계 문서의 열린 결정 사항. 해당 태스크 시작 전까지 결정 필요. -->

| # | 항목 | 영향 범위 | 결정 시한 |
|---|------|----------|----------|
| 1 | {미결 항목} | {Phase/태스크} | {Phase N 시작 전} |

---

## Phase 0 — {Phase 이름}

<!-- 스케일이 프로젝트 전체가 아니면 생략 -->

**목표**: {이 Phase가 끝나면 무엇이 가능한가}

### [INIT-01] · {태스크 이름} (`{파일 경로}`)

**의존** : 없음

{구현 내용 — 무엇을 왜 만드는지}

**Agent 지시** :
{핵심 시그니처, 주의 분기}

**검증 기준** : {단독 완료 판단 조건}

---

### Phase 0 통합 검증

**시나리오**: {한 줄 요약}

1. {실행 명령}
2. {기대 결과 확인}

**합격 조건** : {조건 A} ✅ / {조건 B} ✅

---

## Phase 1 — {Phase 이름}

**목표**: {이 Phase가 끝나면 무엇이 가능한가}

### [CORE-01] · {태스크 이름} (`{파일 경로}`)

**의존** : {선행 태스크 ID}

{구현 내용}

**검증 기준** : {조건}

### [IO-01] · {태스크 이름} (`{파일 경로}`)

**의존** : CORE-01

{구현 내용}

**검증 기준** : {조건}

---

### Phase 1 통합 검증

**시나리오**: {한 줄 요약}

1. {실행 명령}
2. {입력 데이터}
3. {기대 출력 확인}

**합격 조건** : {조건 A} ✅ / {조건 B} ✅ / {조건 C} ✅

---

<!-- Phase N까지 반복 -->

---

## 작업 성격별 태스크 블록 예시

<!-- 실제 산출 시 해당하는 블록만 사용. 모두 쓸 필요 없음. -->

### BE 단일 기능 예시

```markdown
### [DB-01] · 사이트 모델 + 마이그레이션 (`migrations/001_sites.py`)
**의존** : 없음
SQLAlchemy 모델 + 인덱스 + 마이그레이션 스크립트.
**검증 기준** : `alembic upgrade head` 성공 + 롤백 가능

### [CORE-01] · 사이트 등록 도메인 규칙 (`domain/sites.py`)
**의존** : DB-01
이름 중복 검사, 유효 URL 검증.
**검증 기준** : 단위 테스트 통과

### [API-01] · POST /api/sites 라우터 (`api/routers/sites.py`)
**의존** : CORE-01
DTO + 권한 가드 + 도메인 호출 + 에러 매핑.
**검증 기준** : curl happy/validation/권한 3케이스 통과
```

### FE 단일 기능 예시

```markdown
### [UI-01] · SearchResultCard 컴포넌트 (`components/SearchResultCard.tsx`)
**의존** : 없음
props: title, url, snippet. 빈/로딩/에러 상태 분기.
**검증 기준** : 스토리북 4상태 렌더링 + a11y 통과

### [STATE-01] · 검색 결과 훅 (`hooks/useSearchResults.ts`)
**의존** : 없음
react-query 기반 서버 상태 + 캐시 키 + 에러 변환.
**검증 기준** : 훅 단위 테스트 (성공/에러/빈)

### [UI-02] · 검색 결과 페이지 연동 (`pages/search.tsx`)
**의존** : UI-01, STATE-01
훅 호출 + 4상태 라우팅 + 키보드 탐색.
**검증 기준** : E2E 인터랙션 + 반응형 + 접근성
```

---

## 태스크 의존 관계 요약

<!-- 전체 태스크의 의존 관계를 한눈에 보이게 정리 -->

```text
INIT-01
  └─→ CORE-01
        ├─→ IO-01
        └─→ CORE-02
              └─→ TEST-01
                    └─→ PKG-01
```
```
