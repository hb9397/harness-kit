---
name: design-prototype-docs
description: >
  프로토타입용 화면 설계 문서(.md) 생성 스킬. 기능요구사항을 분석하여
  create-prototype 스킬의 입력이 되는 목업 디자인 문서를 생성한다.
  "목업 문서 만들어줘", "화면 설계 문서", "프로토타입 설계", "화면 구성 정리",
  사용자가 Harness Kit의 화면 명세 workflow를 명시적으로 요청하거나 선택한 경우
  "화면 명세 작성", "목업 디자인 문서", "프로토타입 설계" 등에 사용한다.
  결과물은 create-prototype 또는 같은 계약을 따르는 다른 도구에 입력할 수 있는 .md 파일이다.
allowed-tools: Read, Write, Glob, Agent
---

## 문서 루트 계약

이 스킬이 하네스 문서를 읽거나 쓸 때 사용하는 정본은 `.ai-docs/`뿐이다. `.docs/`
디렉토리가 존재해도 참조하지 않고 일반 디렉토리로 취급하며, 호환 별칭으로 추측하지
않는다. 애플리케이션 소스 작업 자체의 권한과 가능 여부는 이 문서 루트 판정으로
제한하지 않는다.


# Design Prototype Docs — 프로토타입 화면 설계 문서 생성기

기능요구사항, RFP/SFR 원문, 사용자가 정리한 요구사항을 분석하여,
`create-prototype` 또는 같은 입력 계약을 따르는 다른 도구에서 사용할 수 있는
**목업 디자인 문서(.md)** 를
생성하는 스킬이다.

저장 경로·소유권·인계는 단일 앱의
`@.ai-docs/instruction/artifact-output-routing-instruction.md` 또는 복수 앱의
`@.ai-docs/{앱}/instruction/artifact-output-routing-instruction.md`를 따른다.

직접 호출할 때는 Codex에서 `$design-prototype-docs`, Claude Code에서
`/harness-kit:design-prototype-docs`를 사용한다.

```
design-prototype-docs OUTPUT (.md)
    └─→ 선택한 prototype producer INPUT  →  HTML/CSS/JS/JSON 프로토타입
```

---

## 워크플로우

### STEP 0 — 플랫폼·실행 방식 확인 + 프로젝트 유형 확인

#### STEP 0-A — 플랫폼·실행 방식 확인

현재 호스트가 독립 작업을 병렬 실행할 수 있는지는 노출된 도구로 확인한다.
관찰 가능한 플랫폼 이름을 사용자에게 다시 묻지 않는다. 작업이 서로 독립적이고
병렬 실행이 실질적으로 유리할 때만 선호를 확인하며, 미지원 또는 미사용 시 순차 실행한다.

#### STEP 0-B — 프로젝트 유형 확인 (C-1 확인 단계)

프로토타입 설계 문서 생성 범위를 확정하기 위해 **반드시** 아래를 수행한다.

1. 현재 수행 위치에서 프로젝트 구조를 탐색한다.
2. **단일 애플리케이션 프로젝트**인지 **복수 애플리케이션 프로젝트**인지 판정한다.
3. 판정 결과 + 적용 대상을 사용자에게 **반드시 재확인**한다.

#### STEP 0-C — 기능 이름/요구사항 ID/RFP ID 확인

폴더명에 적용할 식별자를 **반드시** 사용자에게 확인한다.

> "폴더명에 사용할 식별자를 알려주세요:
> - 제안 단계면 → RFP ID (예: RFP-001)
> - 설계/구현 단계면 → 요구사항 ID (예: SFR-019)
> - 둘 다 없으면 → 기능 이름 (예: bidding-analysis)"

#### STEP 0-D — 사용자 이름 확인

`git config user.name`으로 현재 git 계정을 탐색하고, **반드시** 사용자에게 확인한다.

#### STEP 0-E — 디자인 시스템 입력 확인

화면을 명세하기 전에 참조할 디자인 시스템이 있는지 확인한다.

1. 기존 제품의 디자인 토큰, 테마, 컴포넌트 라이브러리를 찾는다.
2. 단일 앱은 `.ai-docs/design-system/**`, 복수 앱은 `.ai-docs/{앱}/design-system/**`에
   이미 정해진 결정이 있으면 읽고 그대로 따른다.
3. 둘 다 없고 디자인 방향부터 정해야 하면 `ui-ux-pro-max` 결과를 입력으로
   받는다. 이 문서에서 색·타이포그래피·간격 체계를 새로 발명하지 않는다.
4. 확인한 입력의 출처를 문서에 명시한다. 어떤 값이 기존 제품에서 왔고 어떤
   값이 새 제안인지 구분한다.

입력 우선순위는 **기존 제품 디자인 시스템 → 승인된 `ui-ux-pro-max` 결정 →
이 문서의 기본 서술** 순이다. 충돌하면 임의로 정하지 말고 차이를 보고한다.

---

### STEP 1 — 기능요구사항 수집

스킬 호출 시 **반드시 먼저** 기능요구사항을 수집한다.

사용자에게 이렇게 물어본다:
> "프로토타입으로 만들 기능의 요구사항을 알려주세요.
>
> **입력 방법** (아무 방식이든 OK):
> - 파일 첨부 (제안요청서, 기획서, PRD, 노션 export 등)
> - 텍스트로 직접 설명
> - 📌 `@파일경로` 로 문서 위치 알려주기
> - 위 방식을 조합해도 됩니다
>
> 기능요구사항 번호(SFR, REQ 등)가 있으면 함께 알려주세요."

RFP/SFR 원문이 제공되면 별도 사전 스킬을 요구하지 않고 이 스킬 안에서 화면 설계에 필요한 범위만 직접 추출한다. 원문 범위, 요구사항 ID, 필수 기능, 사용자 역할, 제약을 먼저 식별하고 불명확한 항목은 화면 분리 확인 질문에 포함한다.

사용자가 제공한 자료에서 아래 항목을 추출한다:

| 추출 항목 | 설명 |
|---|---|
| 요구사항 번호 | SFR-XXX, REQ-XXX 등 (없으면 기능명에서 추론) |
| 요구사항 명칭 | 공식 명칭 또는 핵심 기능 요약 |
| 프로젝트명 | 소속 프로젝트/시스템명 |
| 핵심 기능 목록 | 제안요청서/기획서에 명시된 세부 기능 전체 |
| 사용자 역할 | 이 화면을 쓰는 사람 (관리자, 실무자, 일반 사용자 등) |

### STEP 2 — 기능 분석 및 화면 분리

수집된 요구사항을 분석하여 **화면 분리 기준**을 도출한다.

**화면 분리 원칙**:
- 요구사항의 **대분류 기능 단위**로 화면을 나눈다
- 하나의 화면 = 하나의 가장 큰 독립 기능 단위
- 사용자가 "이 기능을 수행하기 위해 다른 화면으로 이동할 필요가 없는" 단위
- 화면 간에 데이터 흐름이 있으면 네비게이션 연계로 표현

분석 결과를 사용자에게 확인받는다:
> "요구사항을 분석한 결과, **{N}개 화면**으로 구성하는 것이 적절합니다:
>
> | # | 화면명 | 대분류 기능 | 레이아웃 패턴 |
> |---|---|---|---|
> | 1 | {화면명} | {기능} | {패턴} |
> | ... | ... | ... | ... |
>
> 이 구성으로 진행할까요? 화면을 합치거나 나누고 싶은 부분이 있으면 알려주세요."

### STEP 3 — 메인 색상 수집 (미제공 시에만)

사용자에게 **메인 색상 하나**만 물어본다. 이미 제공된 경우(기존 프로젝트 색상 등) 건너뛴다.

> "디자인의 메인 색상을 알려주세요. (예: #2563EB, 또는 '네이비', '초록' 등)
> 기존 프로젝트 색상 체계가 있으면 그것을 알려주셔도 됩니다."

### STEP 4 — 문서 생성

STEP 1~3에서 수집한 정보를 종합하여 목업 디자인 문서를 생성한다.

**생성 전 반드시 아래 파일을 읽는다:**
- `example/mock_output.md` — 실제 산출물 예시. 섹션 구성, 서술 수준, 표 형식, 도식화 스타일의 기준으로 삼는다.

---

## 문서 구조 (필수 섹션)

생성되는 .md 파일은 아래 구조를 **반드시** 포함해야 한다.

### 1. 기본 정보 테이블

```markdown
| 항목 | 내용 |
|---|---|
| **요구사항 번호** | SFR-020 |
| **요구사항 명칭** | 전기넷 입찰-낙찰정보 분석 |
| **프로젝트명** | `{프로젝트명}` |
| **메인 색상** | `#0D47A1` (딥 네이비) |
| **화면 수** | 5개 |
```

### 2. 요구사항 요약

- 원문 또는 핵심 요약
- 세부 기능 목록 (번호 매긴 리스트)

### 3. 화면 목록 테이블

각 화면에 대해 HTML, CSS, JS, JSON 파일명을 모두 명시한다.

| 화면 번호 | 화면명 | HTML | CSS | JS | JSON | 레이아웃 패턴 |
|---|---|---|---|---|---|---|
| 1 | {화면명} | `display/{PREFIX}-XXX-{slug}.html` | `css/{PREFIX}-XXX-{slug}.css` | `script/{PREFIX}-XXX-{slug}.js` | `data/{PREFIX}-XXX-{slug}-data.json` | {패턴} |

레이아웃 패턴은 create-prototype에서 사용하는 패턴명을 사용한다:
- `대시보드` — 상단 카드 + 차트/테이블
- `앱 셸` — 사이드바 + 메인 + 우측패널
- `목록 화면` — 검색바 + 그리드/테이블
- `마스터-디테일` — 검색 필터 + 목록 + 상세 패널
- `폼 화면` — 라벨 + 입력필드 + 제출
- `풀페이지` — 단일 콘텐츠 영역 (탭 전환 가능)
- `진입 화면` — 중앙 정렬 카드 버튼

### 4. 화면 간 흐름 도식화

**반드시** `text` 코드 블록으로 화면 간 네비게이션 흐름을 도식화한다.

````markdown
```text
[메인 nav 탭]
  ① 수집·적재 현황     ② 분석 워크벤치     ③ 분석 결과 뷰어
  ④ 수익사업 분석      ⑤ 공고·입찰 이력

[화면 간 연결]
  수집·적재 현황(1) → (오류 건 클릭) → 공고·입찰 이력(5)
  분석 워크벤치(2) → (결과 보기 클릭) → 분석 결과 뷰어(3)
  공고·입찰 이력(5) → ([분석에 포함] 클릭) → 분석 워크벤치(2)
```
````

포함 사항:
- 메인 nav 탭 구조
- 화면 간 이동 트리거 (어떤 버튼/링크 클릭 시 어디로 이동)
- 데이터 전달 흐름 (파라미터, 필터 자동 적용 등)

### 5. 화면별 상세 기능 명세 (각 화면마다 반복)

각 화면에 대해 아래 내용을 **모두** 작성한다:

#### 5-1. 목적
- 이 화면이 존재하는 이유 1~2줄
- 어떤 요구사항 항목을 이 화면이 담당하는지

#### 5-2. 화면 내 기능 목록 + 이유

해당 화면에 포함되는 기능과 **왜 이 화면에 배치하는지** 이유를 명시한다.

```markdown
| 기능 | 이유 |
|---|---|
| 일별 수집량 차트 | 수집 추이를 한눈에 파악해야 파이프라인 이상 감지 가능 |
| 오류 로그 재처리 버튼 | 실패 건을 즉시 재시도할 수 있어야 운영 효율 향상 |
```

#### 5-3. 화면 내 기능 흐름 도식화

**반드시** `text` 코드 블록으로 화면 내부의 중분류 기능 흐름을 도식화한다.

````markdown
```text
[화면 2 — 분석 워크벤치 기능 흐름]

  ┌─ 조건 프리셋 선택/등록 ─┐
  │                          │
  ▼                          ▼
  조건 편집 폼            분석 주제 선택/등록
  (기간, 지역, 업종 등)    (주제명, 설명)
  │                          │
  └──────── 합류 ────────────┘
               │
               ▼
         [분석 실행] 버튼
               │
               ▼
         Job 생성 → 상태 추적 (대기→처리중→완료)
               │
               ▼
         [결과 보기] → 화면 3으로 이동
```
````

#### 5-4. 레이아웃 구조

`text` 코드 블록으로 화면 영역 배치를 와이어프레임 수준으로 표현한다.

````markdown
```text
[상단 요약 카드 4개]
  • 오늘 수집 건수 / 총 누적 건수 / 수집 실패 건수 / DB 적재 대기 건수

[중간 2열 영역]
  좌: 일별 수집량 시계열 라인 차트
  우: 수집 채널 상태 카드

[하단 탭]
  탭: 수집 로그 | 적재 로그 | 오류 로그
  테이블: 수집 일시 | 데이터 유형 | 건수 | 상태
```
````

#### 5-5. 주요 컴포넌트 목록

화면에 사용되는 UI 컴포넌트를 bullet 리스트로 나열한다.

#### 5-6. 더미 데이터 예시

프로토타입에 들어갈 사실적인 한국어 더미 데이터 예시를 제공한다.

### 6. 디자인 가이드 (선택)

기존 프로젝트 색상 체계가 있거나, 특별한 디자인 요구가 있으면 작성한다.
없으면 메인 색상만 명시하고 나머지는 create-prototype의 color-system.md에 위임한다.

STEP 0-E에서 디자인 시스템 입력을 받았으면 그 출처와 적용 범위를 여기에 적는다.
값을 다시 정하지 말고 참조만 한다.

### 6-1. 모션 후보 (선택)

이 문서는 모션을 **설계하지 않는다.** 모션이 필요한 **후보와 목적만** 식별한다.

| 화면·요소 | 모션이 필요한 이유 | 목적 분류 |
|---|---|---|
| 예: 결제 버튼 | 처리 중임을 알려야 함 | 상태 변화 |

목적 분류는 정보 전달, 상태 변화, 공간 관계, 피드백, 브랜드 표현 중 하나다.
어디에도 해당하지 않으면 후보로 올리지 않는다.

**정적 화면으로 목적이 충분하면 이 섹션을 비워 둔다.** 빈 섹션은 결함이 아니다.

후보가 있고 사용자가 모션 설계를 원하면 공개 이름 `motion-design`으로 넘긴다.
duration, easing, 속성 같은 구체값을 이 문서에서 정하지 않는다.

### 7. prototype producer 전달 전제조건

이 섹션은 `create-prototype`을 포함한 어떤 prototype producer가 이 문서를 받더라도
따라야 할 구조 규칙을 명시한다.

```text
[산출물 디렉토리 구조]
{PREFIX}-001/
├── display/   ← HTML만
├── data/      ← JSON (원본 보관, fetch 사용 안 함)
├── css/       ← 공통 CSS 1개 + 화면별 CSS 각 1개
├── script/    ← 공통 JS (선택) + 화면별 JS 각 1개 (필수)
└── docs/      ← 이 문서 포함

[파일 짝 규칙 — 화면마다 4개 파일 세트]
- display/{PREFIX}-001-{slug}.html
- css/{PREFIX}-001-{slug}.css
- script/{PREFIX}-001-{slug}.js   ← 반드시 생성, <head>에서 로드
- data/{PREFIX}-001-{slug}-data.json

[데이터 전달 방식]
- JSON 데이터는 <script type="application/json" id="page-data">에 직접 임베드
- fetch() 사용 금지 — file:// 직접 열기 지원
- data/ 폴더 JSON은 원본 보관·diff용으로만 유지

[CSS 규칙]
- <style> 블록 금지
- 공통 CSS: css/{PREFIX}-001.css
- 화면별 CSS: css/{PREFIX}-001-{slug}.css (화면마다 1개 필수)
- 경로: ../css/, ../data/, ../script/ 상대경로만

[JS 로드 순서 규칙]
- 모든 <script src> 선언은 <body>가 아닌 <head>에 위치
- <head> 선언 순서: Tailwind CDN → 공통 CSS → 화면별 CSS → 공통 JS (선택) → 화면별 JS
- 화면별 JS는 반드시 DOMContentLoaded 이벤트 리스너로 DOM 조작 코드를 감싼다
- <body> 내에 로직 <script> 블록 금지 — <script type="application/json" id="page-data">만 허용
- window.{PREFIX} 같은 공통 네임스페이스는 공통 JS에서 정의, 화면별 JS에서 참조
```

### 8. 특이 사항

구현 시 참고할 기술적 특이사항을 나열한다:
- 차트 구현 방식 (더미 SVG/Canvas 등)
- 특수 인터랙션 (타이핑 효과, 프로그레스바 애니메이션 등)
- 외부 연동 시뮬레이션 방식 (alert() 등)
- 화면 간 파라미터 전달 방식

---

## 참조 파일

| 파일 | 내용 | 언제 읽는가 |
|---|---|---|
| `example/mock_output.md` | 실제 산출물 예시 (SFR-021). 섹션 구성·서술 수준·표 형식·도식화 스타일 기준 | STEP 4 시작 시 반드시 |

---

## 파일 저장 규칙

### 식별자 결정

STEP 0-C에서 확인한 식별자를 폴더명에 사용한다.

| 입력 유형 | 폴더명 예시 |
|---|---|
| RFP ID | `RFP-001` |
| 요구사항 ID | `SFR-019` |
| 기능 이름 | `bidding-analysis` (kebab-case) |

### 저장 경로

프로젝트 유형(STEP 0-B)과 사용자(STEP 0-D)에 따라 분기한다:

- **단일 앱**: `.ai-docs/prototype/{사용자}/{식별자}/design-doc.md`
- **복수 앱**: `.ai-docs/{앱}/prototype/{사용자}/{식별자}/design-doc.md`

예시:
- 단일앱: `.ai-docs/prototype/developer/SFR-019/design-doc.md`
- 복수앱: `.ai-docs/portal/prototype/developer/SFR-019/design-doc.md`

### 저장 시 규칙

- 저장 디렉토리가 없으면 생성한다.
- **새로 작성하기 전에, 해당 prototype 경로 하위에 같은 식별자 폴더가 없는지 반드시 확인**한다. 있으면 신규 생성 대신 갱신 여부를 사용자에게 묻는다.
- 같은 경로에 파일이 이미 있으면 새로 만들지 않고 **해당 파일을 갱신**한다.
- **무조건 파일 상단에 갱신일자를 작성/갱신**한다.
- 다른 사용자/git 계정의 문서를 갱신하는 경우, 파일 상단에 **갱신한 사용자 이름 또는 git 계정을 추가 표기**한다.

---

## 품질 기준

1. **prototype producer 입력으로 바로 사용 가능** — 이 문서만 보고 프로토타입을 생성할 수 있어야 한다
2. **화면 분리 근거가 명확** — 각 화면이 왜 독립 화면인지 이유가 있다
3. **기능 흐름이 도식화됨** — 화면 간 흐름 + 화면 내 기능 흐름 모두 `text` 코드 블록으로 시각화
4. **기능 배치 이유가 있음** — 각 기능이 왜 이 화면에 있는지 근거 명시
5. **더미 데이터가 사실적** — 한국어, 실제 업무 맥락에 맞는 구체적 예시
6. **누락 없음** — 요구사항의 모든 세부 기능이 어딘가의 화면에 배치됨
7. **파일명이 완전함** — HTML, CSS, JS, JSON 파일명이 화면 목록 테이블에 모두 명시됨

---

## 문서 개선 후처리와 완료 게이트

직접 호출에서는 다음 실행 컨텍스트를 만들며, 상위 producer가 전달한 값이 있으면
그 값과 소유권을 그대로 사용한다.

```text
artifact_bundle_id = design-prototype-docs:{이번 실행의 고유 ID}
handoff_owner = design-prototype-docs
suppress_child_handoff = false
handoff_completed = false
```

상위 producer가 owner이면 `suppress_child_handoff = true`로 유지하고 이 스킬에서는
별도 후처리를 제안하지 않는다. 직접 호출에서도 사용자가 이번 요청에서 한국어 Markdown
문체 개선을 명시한 경우에만 품질 기준 7개와 prototype 전달 전제조건을 먼저 검증한 뒤,
owner이고 아직 완료되지 않은 bundle에 대해 `humanize-korean`의
`document-refinement` 프로필을 한 번 제안한다. 명시 요청이 없으면 제안하지 않는다.

최종 검증된 Markdown의 정규화 상대경로와 각 파일 SHA-256, profile 이름을 정렬해
`artifact_bundle_fingerprint`를 계산한다. `.ai-docs/.harness/humanize-handoffs.json`
원자적 ledger에서 같은 fingerprint의 완료 상태(`proposed`, `skipped`, `rejected`,
`applied`, `revalidated`)를 찾으면 새 session에서도 재제안하지 않는다. 새 결정은
bundle ID, owner, 파일 hash, 시각과 함께 기록하고 승인 적용 뒤에는 `applied`와
`revalidated`를 순서대로 갱신한다. ledger 자체는 개선 대상에서 제외하며 기록할 수
없으면 현재 session 한정이라고 보고한다.

기본은 proposal-only다. 승인 전에는 산출물 파일을 덮어쓰지 않으며 화면 ID,
요구사항 ID, 라우트, 컴포넌트명, 표, 코드 fence, 수치, 날짜를 보존한다. 사용자가
제안·건너뛰기·거절 중 하나를 결정하면 `handoff_completed = true`와 ledger 상태를
함께 기록한다.

승인된 변경을 반영한 경우 품질 기준 7개, 요구사항 누락 여부, 화면 흐름,
파일명·라우트·표·코드 fence를 다시 검증한다. 재검증된 최종 Markdown만
사용자가 선택한 prototype producer에 전달한다. `create-prototype`은 제공되는
선택지이며 필수 후속 단계가 아니다.
